# Print Layout Manager v4.0 - JSX Version

**ExtendScript (.jsx) версия плагина** для Adobe Photoshop с интеграцией Excel.

## ✨ Преимущества JSX-версии

- ✅ **Простая установка** — скопировать файл и готово
- ✅ **Работает везде** — от CS6 до последних версий
- ✅ **Чтение Excel XLSX** — через библиотеку SheetJS
- ✅ **Привязка к таблице** — каждый принт связан с позицией в Excel
- ✅ **UI панель управления** — выделение, изменение размеров
- ✅ **Без установки UXP** — не нужен Developer Tool

## 📋 Требования

- Adobe Photoshop CS6 или новее
- Библиотека SheetJS для чтения Excel файлов

## 🚀 Установка

### Шаг 1: Скачать библиотеку SheetJS

1. Перейдите на https://cdn.sheetjs.com/xlsx-0.20.3/package/dist/
2. Скачайте файлы:
   - `shim.min.js`
   - `jszip.js`
   - `xlsx.full.min.js`

### Шаг 2: Установить скрипт

1. Скопируйте `PrintLayoutManager_v4.jsx` в папку скриптов Photoshop:
   - **Windows**: `C:\Program Files\Adobe\Adobe Photoshop [версия]\Presets\Scripts\`
   - **macOS**: `/Applications/Adobe Photoshop [версия]/Presets/Scripts/`

2. Скопируйте файлы SheetJS (`shim.min.js`, `jszip.js`, `xlsx.full.min.js`) в ту же папку

3. Откройте `PrintLayoutManager_v4.jsx` в текстовом редакторе

4. Найдите строки (около строки 20):
   ```javascript
   // #include "shim.min.js"
   // #include "jszip.js"
   // #include "xlsx.full.min.js"
   ```

5. Раскомментируйте их (уберите `//`):
   ```javascript
   #include "shim.min.js"
   #include "jszip.js"
   #include "xlsx.full.min.js"
   ```

6. Сохраните файл

### Шаг 3: Запустить

1. Откройте Photoshop
2. Выберите **File → Scripts → PrintLayoutManager_v4**
3. Откроется окно с кнопкой **"Загрузить XLSX"**

## 📖 Использование

### 1. Загрузка таблицы

1. Запустите скрипт: **File → Scripts → PrintLayoutManager_v4**
2. Нажмите **"Загрузить XLSX"**
3. Выберите вашу таблицу Excel
4. Скрипт прочитает данные из столбцов:
   - **A** — Фото (путь к изображению)
   - **B** — Размер (XS, S, M, L, XL, 2XL и т.д.)
   - **C** — ID заказа
   - **D** — Наименование
   - **E** — Цвет
   - **F** — Артикул продавца

### 2. Управление принтами

1. После загрузки Excel нажмите **"Управление принтами"**
2. Откроется панель со списком всех принтов из таблицы
3. Доступные действия:
   - **Выделить** — выделить слой на холсте
   - **Изменить размер** — изменить физический размер принта в мм

### 3. Работа с раскладкой

Скрипт основан на оригинальном "Сборщик v.3.5", поэтому вся логика раскладки работает как обычно:

- Автоматическая упаковка принтов на листы
- Генетический алгоритм оптимизации (5 уровней)
- Поддержка SPB режима
- Сохранение в PDF/TIFF

## ⚙️ Структура таблицы Excel

| A (Фото) | B (Размер) | C (ID) | D (Наименование) | E (Цвет) | F (Артикул) |
|----------|------------|--------|------------------|----------|-------------|
| photo.jpg | XL (50-52) | 123456 | Футболка Троллфейс | белый | www_2096!ya |
| photo2.jpg | M (46-48) | 123457 | Футболка UFC | черный | pek_04468!ya |

**Важно:**
- Первая строка — заголовки (будет пропущена)
- Столбец F (артикул) используется для связи с принтами
- Столбец B (размер) помогает идентифицировать файлы

## 🔧 Как это работает

1. **Загрузка Excel** — SheetJS читает .xlsx файл и парсит данные
2. **Размещение принтов** — скрипт размещает файлы из папок на холст
3. **Привязка данных** — каждому принту присваивается `layerId` и связь с Excel
4. **Управление** — через UI панель можно выделять и изменять размеры

## ⚠️ Известные ограничения

1. **Требуется SheetJS** — без библиотеки Excel не будет работать
2. **Нет превью фото** — столбец A (фото) пока не используется
3. **Ручная установка** — нужно скопировать файлы вручную
4. **Простой UI** — ScriptUI менее красив чем HTML

## 🆚 Сравнение с UXP версией

| Функция | JSX | UXP |
|---------|-----|-----|
| Установка | Копирование файла | Через Developer Tool |
| Версии PS | CS6+ | 2024+ |
| UI | ScriptUI (простой) | HTML/CSS (красивый) |
| Excel | Через SheetJS | Через SheetJS (CDN) |
| Синхронизация | Нет realtime | Есть realtime (500ms) |
| Стабильность | Высокая | Зависит от UXP |

## 🐛 Troubleshooting

### Ошибка "XLSX is not defined"

**Решение:** Убедитесь, что:
1. Файлы `shim.min.js`, `jszip.js`, `xlsx.full.min.js` находятся в папке Scripts
2. Строки `#include` раскомментированы
3. Имена файлов точно совпадают

### Не загружается таблица

**Решение:**
1. Убедитесь, что файл .xlsx не поврежден
2. Откройте его в Excel и пересохраните
3. Проверьте, что первая строка — заголовки

### Принты не привязываются

**Решение:**
- Скрипт ищет артикул (столбец F) в имени файла принта
- Убедитесь, что файлы содержат артикул в названии
- Например: `pek_04468_M.pdf` для артикула `pek_04468!ya`

## 📄 Лицензия

MIT License — свободное использование и модификация.

---

**Версия:** 4.0 JSX  
**Дата:** Ноябрь 2025  
**Базируется на:** Сборщик v.3.5